<!doctype html>
<html lang="vi">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Twig SSTI Lab</title>
  <style>
    body {
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background: #0f172a;
      color: #e2e8f0;
      margin: 0;
    }

    .wrap {
      max-width: 860px;
      margin: 0 auto;
      padding: 24px;
    }

    h1 {
      font-weight: 700;
      font-size: 1.6rem;
      margin: 0 0 0.5rem;
    }

    h2 {
      font-size: 1.2rem;
      margin: 1rem 0;
    }

    .note {
      opacity: .9;
      font-size: .95rem;
      display: flex;
      gap: 12px;
      align-items: center;
      flex-wrap: wrap
    }

    form {
      display: flex;
      gap: 8px;
    }

    input[type=text] {
      padding: 10px;
      border-radius: 10px;
      border: 1px solid #334155;
      background: #0b1220;
      color: #e2e8f0;
      width: 100%;
    }

    button,
    a.btn {
      padding: 10px 16px;
      border: 0;
      border-radius: 10px;
      background: #22c55e;
      color: #0b1220;
      font-weight: 600;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
    }

    a.btn-outline {
      background: transparent;
      border: 1px solid #334155;
      color: #e2e8f0;
    }

    .card {
      background: #0b1220;
      border: 1px solid #334155;
      border-radius: 16px;
      padding: 16px;
      margin-top: 16px;
    }

    .result {
      white-space: pre-wrap;
      word-break: break-word;
      margin-top: 8px;
    }

    a.btn,
    a.btn-outline {
      transition: transform .12s ease, box-shadow .12s ease, background-color .2s ease, color .2s ease, border-color .2s ease;
    }

    a.btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 6px 18px rgba(34, 197, 94, .25);
    }

    a.btn-outline:hover {
      background: #22c55e;
      color: #0b1220;
      border-color: #22c55e;
      transform: translateY(-1px);
      box-shadow: 0 6px 18px rgba(34, 197, 94, .20);
    }
  </style>
</head>

<body>
  <div class="wrap">
    <h1>Twig SSTI Lab</h1>
    <div class="note">
      <a href="/safe.php" class="btn btn-outline">Trang an toàn</a>
    </div>
    {% block content %}{% endblock %}
  </div>
</body>

</html>